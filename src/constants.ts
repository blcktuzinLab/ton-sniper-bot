/** STON.fi native TON representation for swap API. */
export const STON_NATIVE_MASTER = "EQAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAM9c";

export const DEFAULT_SLIPPAGE = "0.05";

/** Poll interval while waiting for liquidity (ms). */
export const DEFAULT_POLL_MS = 250;

/** Max time to wait for a routable pool (ms). */
export const DEFAULT_MAX_WAIT_MS = 600_000;
